import moment from 'moment';
import { Request, Response, NextFunction } from 'express';
import { feed } from "./../services/neo.api.service";

export class RollUpController {
  async rollup(req: Request, res: Response, next: NextFunction) {

  }
}
